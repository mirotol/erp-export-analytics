// Package httpapi provides the HTTP handlers and routing for the API.
// It acts as the entry point for all external requests, coordinating with
// other internal packages to process uploads, manage samples, and run reports.
package httpapi
